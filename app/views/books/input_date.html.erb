<h1>予約</h1>
<p>予約する日付をクリックしてください。</p>
<%= month_calendar do |date| %>
  <%= date.day %>
	  <% if  date > Time.current.yesterday %>
	  	<div style="text-align:center">
	  	<%= button_to '予約', books_input_date_path(book: {date: date}) %>
	  	</div>
	  <%end%>
<% end %>
